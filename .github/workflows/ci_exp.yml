name: CI Experiment

on:
  pull_request:
  push:
    branches:
      - main
      - master

jobs:
  job1-full-log-reproduction:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Simulated Full OpenWISP Log
        run: |
          cat << 'EOF' > mock_log_output.txt
          Found 107 test(s).
          Creating test database for alias 'default'...
          Cloning test database for alias 'default'...
          Cloning test database for alias 'default'...
          Cloning test database for alias 'default'...
          Cloning test database for alias 'default'...
          System check identified no issues (0 silenced).
          .........................................................../opt/hostedtoolcache/Python/3.10.20/x64/lib/python3.10/site-packages/django/contrib/admin/options.py:913: UnorderedObjectListWarning: Pagination may yield inconsistent results with an unordered object_list: <class 'test_project.models.Book'> QuerySet.
            return self.paginator(queryset, per_page, orphans, allow_empty_first_page)
          ................................................

          Summary of slow tests (>0.3s)


          Total slow tests detected: 0
          ----------------------------------------------------------------------
          Ran 107 tests in 10.067s

          OK
          Destroying test database for alias 'default'...
          Destroying test database for alias 'default'...
          Destroying test database for alias 'default'...
          Destroying test database for alias 'default'...
          Destroying test database for alias 'default'...
          ============================= test session starts ==============================
          platform linux -- Python 3.10.20, pytest-9.0.3, pluggy-1.6.0
          rootdir: /home/runner/work/openwisp-utils/openwisp-utils
          plugins: mock-3.15.1, anyio-4.13.0
          collected 360 items

          openwisp_utils/releaser/tests/test_ai_summary.py ........                [  2%]
          openwisp_utils/releaser/tests/test_changelog.py ...................      [  7%]
          openwisp_utils/releaser/tests/test_commitizen_rules.py ................. [ 12%]
          ....                                                                     [ 13%]
          openwisp_utils/releaser/tests/test_config.py ........................... [ 20%]
          .....                                                                    [ 22%]
          openwisp_utils/releaser/tests/test_github.py .......                     [ 24%]
          openwisp_utils/releaser/tests/test_release.py ......................     [ 30%]
          openwisp_utils/releaser/tests/test_utils.py ................             [ 34%]
          openwisp_utils/releaser/tests/test_version_bumping.py .................. [ 39%]
          ......                                                                   [ 41%]
          .github/actions/bot-ci-failure/test_analyze_failure.py ................. [ 46%]
          .......................................FFF....FF.F                       [ 60%]
          .github/actions/bot-changelog-generator/test_generate_changelog.py ..... [ 61%]
          ...................................................                      [ 75%]
          .github/actions/bot-autoassign/tests/test_issue_assignment_bot.py ...... [ 77%]
          .............................................                            [ 89%]
          .github/actions/bot-autoassign/tests/test_pr_reopen_bot.py ............. [ 93%]
                                                                                   [ 93%]
          .github/actions/bot-autoassign/tests/test_stale_pr_bot.py .............. [ 97%]
          ..........                                                               [100%]

          =================================== FAILURES ===================================
          ____________________ TestMain.test_fails_format_validation _____________________

          self = <MagicMock name='print' id='140516266795808'>
          args = ('::warning::LLM output failed format validation; skipping comment.',)
          kwargs = {'file': <_io.TextIOWrapper name="<_io.FileIO name=10 mode='rb+' closefd=True>" mode='r+' encoding='utf-8'>}
          msg = 'Expected \'print\' to be called once. Called 2 times.\nCalls: [call(\'::notice::Retry mode=llm transient_only=False s...t.\', file=<_io.TextIOWrapper name="<_io.FileIO name=10 mode=\'rb+\' closefd=True>" mode=\'r+\' encoding=\'utf-8\'>)].'

              def assert_called_once_with(self, /, *args, **kwargs):
                  """assert that the mock was called exactly once and that that call was
                  with the specified arguments."""
                  if not self.call_count == 1:
                      msg = ("Expected '%s' to be called once. Called %s times.%s"
                             % (self._mock_name or 'mock',
                                self.call_count,
                                self._calls_repr()))
          >           raise AssertionError(msg)
          E           AssertionError: Expected 'print' to be called once. Called 2 times.
          E           Calls: [call('::notice::Retry mode=llm transient_only=False should_retry=False', file=<_io.TextIOWrapper name="<_io.FileIO name=10 mode='rb+' closefd=True>" mode='r+' encoding='utf-8'>),
          E            call('::warning::LLM output failed format validation; skipping comment.', file=<_io.TextIOWrapper name="<_io.FileIO name=10 mode='rb+' closefd=True>" mode='r+' encoding='utf-8'>)].

          /opt/hostedtoolcache/Python/3.10.20/x64/lib/python3.10/unittest/mock.py:940: AssertionError

          During handling of the above exception, another exception occurred:

          self = <test_analyze_failure.TestMain testMethod=test_fails_format_validation>
          mock_retry = <MagicMock name='_should_retry_ci' id='140516266796288'>
          mock_repo = <MagicMock name='get_repo_context' id='140516268141264'>
          mock_logs = <MagicMock name='get_error_logs' id='140516268137424'>
          mock_genai = <MagicMock name='genai' id='140516270004816'>
          mock_print = <MagicMock name='print' id='140516266795808'>

              @patch("builtins.print")
              @patch("analyze_failure.genai")
              @patch("analyze_failure.get_error_logs")
              @patch("analyze_failure.get_repo_context")
              @patch("analyze_failure._should_retry_ci")
              @patch.dict(
                  os.environ,
                  {"GEMINI_API_KEY": "fake_key", "PR_AUTHOR": "test", "COMMIT_SHA": "abc"},
              )
              def test_fails_format_validation(
                  self, mock_retry, mock_repo, mock_logs, mock_genai, mock_print
              ):
                  mock_logs.return_value = ("Fake error log", True, False)
                  mock_repo.return_value = "Code"
                  mock_retry.return_value = False
                  mock_client = MagicMock()
                  mock_response = MagicMock()
                  mock_response.text = "Here is how to fix the bug."
                  mock_client.models.generate_content.return_value = mock_response
                  mock_genai.Client.return_value = mock_client
                  with self.assertRaises(SystemExit) as context:
                      main()
                  self.assertEqual(context.exception.code, 0)
          >       mock_print.assert_called_once_with(
                      "::warning::LLM output failed format validation; skipping comment.",
                      file=sys.stderr,
                  )
          E       AssertionError: Expected 'print' to be called once. Called 2 times.
          E       Calls: [call('::notice::Retry mode=llm transient_only=False should_retry=False', file=<_io.TextIOWrapper name="<_io.FileIO name=10 mode='rb+' closefd=True>" mode='r+' encoding='utf-8'>),
          E        call('::warning::LLM output failed format validation; skipping comment.', file=<_io.TextIOWrapper name="<_io.FileIO name=10 mode='rb+' closefd=True>" mode='r+' encoding='utf-8'>)].

          .github/actions/bot-ci-failure/test_analyze_failure.py:694: AssertionError
          _____________________ TestMain.test_handles_api_exception ______________________

          self = <MagicMock name='print' id='140516267118304'>
          args = ('::warning::API Error (Max retries reached or fatal error): Quota Exceeded',)
          kwargs = {'file': <_io.TextIOWrapper name="<_io.FileIO name=10 mode='rb+' closefd=True>" mode='r+' encoding='utf-8'>}
          msg = 'Expected \'print\' to be called once. Called 2 times.\nCalls: [call(\'::notice::Retry mode=llm transient_only=False s...ed\', file=<_io.TextIOWrapper name="<_io.FileIO name=10 mode=\'rb+\' closefd=True>" mode=\'r+\' encoding=\'utf-8\'>)].'

              def assert_called_once_with(self, /, *args, **kwargs):
                  """assert that the mock was called exactly once and that that call was
                  with the specified arguments."""
                  if not self.call_count == 1:
                      msg = ("Expected '%s' to be called once. Called %s times.%s"
                             % (self._mock_name or 'mock',
                                self.call_count,
                                self._calls_repr()))
          >           raise AssertionError(msg)
          E           AssertionError: Expected 'print' to be called once. Called 2 times.
          E           Calls: [call('::notice::Retry mode=llm transient_only=False should_retry=False', file=<_io.TextIOWrapper name="<_io.FileIO name=10 mode='rb+' closefd=True>" mode='r+' encoding='utf-8'>),
          E            call('::warning::API Error (Max retries reached or fatal error): Quota Exceeded', file=<_io.TextIOWrapper name="<_io.FileIO name=10 mode='rb+' closefd=True>" mode='r+' encoding='utf-8'>)].

          /opt/hostedtoolcache/Python/3.10.20/x64/lib/python3.10/unittest/mock.py:940: AssertionError

          During handling of the above exception, another exception occurred:

          self = <test_analyze_failure.TestMain testMethod=test_handles_api_exception>
          mock_retry = <MagicMock name='_should_retry_ci' id='140516267170624'>
          mock_repo = <MagicMock name='get_repo_context' id='140516267175568'>
          mock_logs = <MagicMock name='get_error_logs' id='140516266756656'>
          mock_genai = <MagicMock name='genai' id='140516267126752'>
          mock_print = <MagicMock name='print' id='140516267118304'>

              @patch("builtins.print")
              @patch("analyze_failure.genai")
              @patch("analyze_failure.get_error_logs")
              @patch("analyze_failure.get_repo_context")
              @patch("analyze_failure._should_retry_ci")
              @patch.dict(os.environ, {"GEMINI_API_KEY": "fake_key"})
              def test_handles_api_exception(
                  self, mock_retry, mock_repo, mock_logs, mock_genai, mock_print
              ):
                  mock_logs.return_value = ("Error log", True, False)
                  mock_repo.return_value = "Code"
                  mock_retry.return_value = False
                  mock_client = MagicMock()
                  mock_client.models.generate_content.side_effect = Exception("Quota Exceeded")
                  mock_genai.Client.return_value = mock_client
                  with self.assertRaises(SystemExit):
                      main()
          >       mock_print.assert_called_once_with(
                      "::warning::API Error (Max retries reached or fatal error): Quota Exceeded",
                      file=sys.stderr,
                  )
          E       AssertionError: Expected 'print' to be called once. Called 2 times.
          E       Calls: [call('::notice::Retry mode=llm transient_only=False should_retry=False', file=<_io.TextIOWrapper name="<_io.FileIO name=10 mode='rb+' closefd=True>" mode='r+' encoding='utf-8'>),
          E        call('::warning::API Error (Max retries reached or fatal error): Quota Exceeded', file=<_io.TextIOWrapper name="<_io.FileIO name=10 mode='rb+' closefd=True>" mode='r+' encoding='utf-8'>)].

          .github/actions/bot-ci-failure/test_analyze_failure.py:740: AssertionError
          ___________________ TestMain.test_handles_empty_api_response ___________________

          self = <MagicMock name='print' id='140516272945952'>
          args = ('::warning::Generation returned an empty response; skipping report.',)
          kwargs = {'file': <_io.TextIOWrapper name="<_io.FileIO name=10 mode='rb+' closefd=True>" mode='r+' encoding='utf-8'>}
          msg = 'Expected \'print\' to be called once. Called 2 times.\nCalls: [call(\'::notice::Retry mode=llm transient_only=False s...t.\', file=<_io.TextIOWrapper name="<_io.FileIO name=10 mode=\'rb+\' closefd=True>" mode=\'r+\' encoding=\'utf-8\'>)].'

              def assert_called_once_with(self, /, *args, **kwargs):
                  """assert that the mock was called exactly once and that that call was
                  with the specified arguments."""
                  if not self.call_count == 1:
                      msg = ("Expected '%s' to be called once. Called %s times.%s"
                             % (self._mock_name or 'mock',
                                self.call_count,
                                self._calls_repr()))
          >           raise AssertionError(msg)
          E           AssertionError: Expected 'print' to be called once. Called 2 times.
          E           Calls: [call('::notice::Retry mode=llm transient_only=False should_retry=False', file=<_io.TextIOWrapper name="<_io.FileIO name=10 mode='rb+' closefd=True>" mode='r+' encoding='utf-8'>),
          E            call('::warning::Generation returned an empty response; skipping report.', file=<_io.TextIOWrapper name="<_io.FileIO name=10 mode='rb+' closefd=True>" mode='r+' encoding='utf-8'>)].

          /opt/hostedtoolcache/Python/3.10.20/x64/lib/python3.10/unittest/mock.py:940: AssertionError

          During handling of the above exception, another exception occurred:

          self = <test_analyze_failure.TestMain testMethod=test_handles_empty_api_response>
          mock_retry = <MagicMock name='_should_retry_ci' id='140516566899232'>
          mock_repo = <MagicMock name='get_repo_context' id='140516273594016'>
          mock_logs = <MagicMock name='get_error_logs' id='140516273611744'>
          mock_genai = <MagicMock name='genai' id='140516273618656'>
          mock_print = <MagicMock name='print' id='140516272945952'>

              @patch("builtins.print")
              @patch("analyze_failure.genai")
              @patch("analyze_failure.get_error_logs")
              @patch("analyze_failure.get_repo_context")
              @patch("analyze_failure._should_retry_ci")
              @patch.dict(os.environ, {"GEMINI_API_KEY": "fake_key"})
              def test_handles_empty_api_response(
                  self, mock_retry, mock_repo, mock_logs, mock_genai, mock_print
              ):
                  mock_logs.return_value = ("Error log", True, False)
                  mock_repo.return_value = "Code"
                  mock_retry.return_value = False
                  mock_client = MagicMock()
                  mock_response = MagicMock()
                  mock_response.text = "   \n  "
                  mock_client.models.generate_content.return_value = mock_response
                  mock_genai.Client.return_value = mock_client
                  with self.assertRaises(SystemExit):
                      main()
          >       mock_print.assert_called_once_with(
                      "::warning::Generation returned an empty response; skipping report.",
                      file=sys.stderr,
                  )
          E       AssertionError: Expected 'print' to be called once. Called 2 times.
          E       Calls: [call('::notice::Retry mode=llm transient_only=False should_retry=False', file=<_io.TextIOWrapper name="<_io.FileIO name=10 mode='rb+' closefd=True>" mode='r+' encoding='utf-8'>),
          E        call('::warning::Generation returned an empty response; skipping report.', file=<_io.TextIOWrapper name="<_io.FileIO name=10 mode='rb+' closefd=True>" mode='r+' encoding='utf-8'>)].

          .github/actions/bot-ci-failure/test_analyze_failure.py:718: AssertionError
          __________________ TestMain.test_strips_wrapping_code_fences ___________________

          self = <MagicMock name='print' id='140516270855440'>
          args = ('### Test Failed\nHello @testuser\n*(Analysis for commit abc1234)*\nHere is the fix.',)
          kwargs = {}
          msg = "Expected 'print' to be called once. Called 2 times.\nCalls: [call('::notice::Retry mode=llm transient_only=False shou...encoding='utf-8'>),\n call('### Test Failed\\nHello @testuser\\n*(Analysis for commit abc1234)*\\nHere is the fix.')]."

              def assert_called_once_with(self, /, *args, **kwargs):
                  """assert that the mock was called exactly once and that that call was
                  with the specified arguments."""
                  if not self.call_count == 1:
                      msg = ("Expected '%s' to be called once. Called %s times.%s"
                             % (self._mock_name or 'mock',
                                self.call_count,
                                self._calls_repr()))
          >           raise AssertionError(msg)
          E           AssertionError: Expected 'print' to be called once. Called 2 times.
          E           Calls: [call('::notice::Retry mode=llm transient_only=False should_retry=False', file=<_io.TextIOWrapper name="<_io.FileIO name=10 mode='rb+' closefd=True>" mode='r+' encoding='utf-8'>),
          E            call('### Test Failed\nHello @testuser\n*(Analysis for commit abc1234)*\nHere is the fix.')].

          /opt/hostedtoolcache/Python/3.10.20/x64/lib/python3.10/unittest/mock.py:940: AssertionError

          During handling of the above exception, another exception occurred:

          self = <test_analyze_failure.TestMain testMethod=test_strips_wrapping_code_fences>
          mock_retry = <MagicMock name='_should_retry_ci' id='140516270140208'>
          mock_repo = <MagicMock name='get_repo_context' id='140516264255808'>
          mock_logs = <MagicMock name='get_error_logs' id='140516268550960'>
          mock_genai = <MagicMock name='genai' id='140516268546928'>
          mock_print = <MagicMock name='print' id='140516270855440'>

              @patch("builtins.print")
              @patch("analyze_failure.genai")
              @patch("analyze_failure.get_error_logs")
              @patch("analyze_failure.get_repo_context")
              @patch("analyze_failure._should_retry_ci")
              @patch.dict(
                  os.environ,
                  {
                      "GEMINI_API_KEY": "fake_key",
                      "PR_AUTHOR": "testuser",
                      "COMMIT_SHA": "abc1234",
                  },
              )
              def test_strips_wrapping_code_fences(
                  self, mock_retry, mock_repo, mock_logs, mock_genai, mock_print
              ):
                  mock_logs.return_value = ("Fake error log", True, False)
                  mock_repo.return_value = "<file path='test.py'>code</file>"
                  mock_retry.return_value = False
                  mock_client = MagicMock()
                  mock_response = MagicMock()
                  mock_response.text = (
                      "```markdown\n"
                      "### Test Failed\n"
                      "Hello @testuser\n"
                      "*(Analysis for commit abc1234)*\n"
                      "Here is the fix.\n"
                      "```"
                  )
                  mock_client.models.generate_content.return_value = mock_response
                  mock_genai.Client.return_value = mock_client
    
